# HernándezStore con Cloudinary

## Instalación
```bash
npm install
cp .env.example .env
# editar .env con tus credenciales
npm start
```

## Endpoints
- `/` → tienda
- `/admin` → panel admin con subida de imágenes
- `/upload` → endpoint para subir a Cloudinary
